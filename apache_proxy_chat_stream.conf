# Chat Stream API proxy configuration
# Add these lines to /etc/httpd/conf.d/ssl.conf after line 59 (after the satisfaction proxy lines)

# Chat Stream API proxy
ProxyPass /api/chat_stream http://localhost:8010/api/chat_stream
ProxyPassReverse /api/chat_stream http://localhost:8010/api/chat_stream

# Then restart Apache:
# sudo systemctl restart httpd
